spring:
  profiles:
    active: local
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    generate-ddl: true

server:
  servlet:
    context-path: /api/v1
  port: 8081